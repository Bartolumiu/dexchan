services:
  dexchan_dev:
    build: .
    container_name: dexchan_dev
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - BOT_TOKEN=${DEXCHAN_DEV_TOKEN}
      - DB_URI=${DB_URI}
      - CLIENT_ID=${CLIENT_ID}