minimumReleaseAge: 10080 # 7 * 24 * 60 / Minimum age of a release in minutes (7 days)

allowBuilds:
  "@prisma/engines": true
  prisma: true
  unrs-resolver: false
  esbuild: true

overrides:
  babel-plugin-istanbul: "~8.0.0"
  test-exclude: "~8.0.0"
  glob: "~13.0.6"
